let
    var start:=0
    var en:=0
    var result := 0
    var i:=23
    function fib(n)=(if n < 2 then 1 else fib(n - 1)+fib(n-2))

in
    for a:=1 to 10 do(
        start := nano_time();
        fib(30);
        en := nano_time();
        print((en - start)/1000000)
    )
end